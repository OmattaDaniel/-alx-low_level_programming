#include "main.h"
#include <stdio.h>

/**
 * main - prints program name
 * @argc: number of arguments
 * @argv: array pointing to argc
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
